# Binaries
bin/
/api
coverage.out
coverage.html

# Environment & secrets
.env
.env.local

# IDE
.idea/
*.swp

# OS
.DS_Store

# Go workspace
go.work.sum

.hermes/

# Local planning docs (roadmaps, specs, TODOs, strategy) live under .docs/
# and are not part of the public repo. The whole directory is ignored so
# future planning files dropped in there need no .gitignore edits.
.docs/
